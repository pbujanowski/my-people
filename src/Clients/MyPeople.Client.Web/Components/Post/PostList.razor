<MudStack>
    @foreach (var post in Posts)
    {
        <PostItem
            Post="post"
            CurrentUserId="CurrentUserId"
            EditPost="async () => await EditPostAsync(post)"
            RemovePost="async () => await RemovePostAsync(post)"/>
    }
</MudStack>
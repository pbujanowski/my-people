<MudStack>

    @foreach (var post in Posts)
    {
        <PostItem Post="post" CurrentUserId="@GetCurrentUserId()"
            OnPostEdit="() => GetAllPostsAsync()" OnPostRemove="() => GetAllPostsAsync()" />
    }

</MudStack>
